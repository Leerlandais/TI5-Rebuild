<!-- Topic Nav -->
<nav class="w-full py-4 border-t border-b bg-gray-100" x-data="{ open: false }">
    <div class="block sm:hidden">
        <a
                href="#"
                class="block md:hidden text-base font-bold uppercase text-center flex justify-center items-center"
                @click="open = !open"
        >
            Topics <i :class="open ? 'fa-chevron-down': 'fa-chevron-up'" class="fas ml-2"></i>
        </a>
    </div>
    <div :class="open ? 'block': 'hidden'" class="w-full flex-grow sm:flex sm:items-center sm:w-auto">
        <div class="w-full container mx-auto flex flex-col sm:flex-row items-center justify-center text-sm font-bold uppercase mt-0 px-6 py-2">
            {% if is_granted("ROLE_SUPER") %}
            {% endif %}
            {% if is_granted("ROLE_ADMIN") %}
            {% endif %}
            {% if is_granted("ROLE_REDAC") %}
                <ul class="flex flex-row">
                    <li><a class="hover:text-gray-200 hover:underline px-4" href="{{ path("app_article_new") }}">Add Article</a></li>
                    <li><a class="hover:text-gray-200 hover:underline px-4" href="{{ path("app_article_index") }}">Edit Your Articles</a></li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>