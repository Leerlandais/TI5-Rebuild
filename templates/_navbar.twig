    <nav class="w-full py-4 bg-blue-800 shadow">
        <div class="w-full container mx-auto flex flex-wrap items-center justify-between">
            {% block navMain %}
                <nav>
                    <ul class="flex items-center justify-between font-bold text-sm text-white uppercase no-underline">
                        {% if is_granted("IS_AUTHENTICATED") %}
                            <li><a class="hover:text-gray-200 hover:underline px-4" href="{{ path("app_logout") }}">Logout</a></li>
                        {% else %}
                            <li><a class="hover:text-gray-200 hover:underline px-4" href="{{ path("app_login") }}">Login</a></li>
                            <li><a class="hover:text-gray-200 hover:underline px-4" href="{{ path('public_user_new') }}">Signup</a></li>
                        {% endif %}
                        {% if is_granted("ROLE_SUPER") %}
                            <li><a class="hover:text-gray-200 hover:underline px-4" href="{{ path("app_logout") }}">Super Controls</a></li>
                        {% endif %}
                        {% if is_granted("ROLE_ADMIN") %}
                            <li><a class="hover:text-gray-200 hover:underline px-4" href="{{ path("app_logout") }}">Admin Controls</a></li>
                        {% endif %}
                        {% if is_granted("ROLE_REDAC") %}
                            <li><a class="hover:text-gray-200 hover:underline px-4" href="{{ path("app_article_index") }}">Redac Controls</a></li>
                        {% endif %}
                    </ul>
                </nav>
            {% endblock %}
            {% block navSide %}
                <div class="flex items-center text-lg no-underline text-white pr-6">
                    <a class="" href="#" title="Decorative Link">
                        <i class="fab fa-facebook"></i>
                    </a>
                    <a class="pl-6" href="#" title="Decorative Link">
                        <i class="fab fa-instagram"></i>
                    </a>
                    <a class="pl-6" href="#" title="Decorative Link">
                        <i class="fab fa-twitter"></i>
                    </a>
                    <a class="pl-6" href="#" title="Decorative Link">
                        <i class="fab fa-linkedin"></i>
                    </a>
                </div>
            {% endblock %}
        </div>
    </nav>